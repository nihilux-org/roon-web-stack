export interface ZoneProgression {
  length: string;
  position: string;
  percentage: number;
}

export const DEFAULT_ZONE_PROGRESSION: ZoneProgression = {
  length: "-",
  position: "-",
  percentage: 0,
};
